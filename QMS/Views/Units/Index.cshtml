
@using QMS.Core.Resources
@using QMS.Data
@using GridMvc.Html
@model IEnumerable<QMS.Data.Unit>

@{
    ViewBag.Title = CommonRes.Index;
}

<h2>@CommonRes.Index</h2>

<div style="text-align: right;">
    <form action="~/Units/Create">
        <button type="submit" class="btn btn-default alignPreview">
            <span class="glyphicon glyphicon-new-window"></span>
            @CommonRes.Button_CreateNew
        </button>
    </form>
</div>




<br />
@Html.Grid(Model).Named("ordersGrid").Columns(columns =>
{
		columns.Add(model => model.Unit_ID).Sortable(true).SetWidth(50).Titled(QMSRes.Unit_ID);
	
    	columns.Add(model => model.Unit_Name).Sortable(true).SetWidth(50).Titled(QMSRes.Unit_Name);
	
    	columns.Add(model => model.Unit_Name_AR).Sortable(true).SetWidth(50).Titled(QMSRes.Unit_Name_AR);
	
    	columns.Add(model => model.Comment).Sortable(true).SetWidth(50).Titled(QMSRes.Comment);
	
    	columns.Add(model => model.Status).Sortable(true).SetWidth(50).Titled(QMSRes.Status);
	
    	columns.Add(model => model.LastUpdate).Sortable(true).SetWidth(50).Titled(QMSRes.LastUpdate);
	
    	columns.Add(model => model.Entered_By).Sortable(true).SetWidth(50).Titled(QMSRes.Entered_By);
	
          columns.Add().Encoded(false).Sanitized(false).RenderValueAs(o => Html.ActionLink(CommonRes.Link_Edit, "Edit", new { id = o.Unit_ID })).SetWidth(50);
                columns.Add().Encoded(false).Sanitized(false).RenderValueAs(o => Html.ActionLink(CommonRes.Link_Detail, "Details", new { id = o.Unit_ID })).SetWidth(50);
                columns.Add().Encoded(false).Sanitized(false).RenderValueAs(o => Html.ActionLink(CommonRes.Link_Delete, "Delete", new { id = o.Unit_ID })).SetWidth(50);
    
}).WithPaging(10).Filterable().SetLanguage(CommonRes.Lang)


